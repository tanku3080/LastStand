using UnityEngine;
using UnityEngine.UI;

public class CharactorSet : MonoBehaviour
{
    [SerializeField] Button[] buo;
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void ObjectOnset()
    {

    }
}
